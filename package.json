{
  "name": "smartroute",
  "version": "1.0.0",
  "description": "SmartRoute - Intelligent Traffic, Navigation & Dynamic Route Management System",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"blue,cyan\" \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "client": "npm run dev --workspace=client",
    "server": "npm run dev --workspace=server",
    "build": "npm run build --workspace=client && node -e \"const fs=require('fs'); fs.cpSync('client/dist', 'dist', {recursive: true});\"",
    "start": "npm run start --workspace=server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "keywords": [
    "smartroute",
    "traffic",
    "navigation",
    "smart-city",
    "iot",
    "dynamic-routing"
  ],
  "author": "SmartRoute Innovation Team",
  "license": "MIT"
}
